# Ambiente / segredos — nunca versionar .env real
.env
.env.*
!.env.example

# Terraform — nunca versionar tfvars com credenciais
terraform/.terraform/
terraform/terraform.tfvars
terraform/*.tfstate
terraform/*.tfstate.backup

# Builds e artefatos
bin/
dist/
tmp/
*.log

# Ferramentas
.vscode/
.idea/
